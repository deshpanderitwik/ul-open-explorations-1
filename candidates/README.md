# candidates/

Scratch space for in-flight candidate crates (clones of `engine/`). Generated
candidates (`gen-*`) are gitignored — only the mainline `engine/` is tracked.
The run history lives in `archive/runs.jsonl`.
